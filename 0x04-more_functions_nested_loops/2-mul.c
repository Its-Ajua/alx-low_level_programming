#include "main.h"
/**
 * mul - multiply two integers
 * @a: first integer to be multiplied
 * @b: second integer to be multipled
 * Return: result of the two multiplied integers.
 */

int mul(int a, int b)
{
	int n = a * b;

	return (n);
}
